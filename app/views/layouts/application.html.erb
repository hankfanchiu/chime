<!DOCTYPE html>
<html>
<head>
  <title>Chime &mdash; Let's Chime In</title>

  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>

  <script>
    <% if logged_in? %>
      window.currentUserId = <%= current_user.id %>;
    <% else %>
      window.currentUserId = null;
    <% end %>
  </script>

</head>
<body>

<%= yield %>

</body>
</html>
